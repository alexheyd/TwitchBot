<i class="fa fa-smile-o" {{action "toggleEmojiSelector"}}></i>

<ul class="{{emojiSelectorClass}}">
  {{#each-in emojis as |code emoji|}}
    <li><img src="{{emoji.imageUrl}}" {{action "useEmoji" code}} /></li>
  {{/each-in}}
</ul>
